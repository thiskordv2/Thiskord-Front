<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Thiskord_Front.Views.ChannelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Thiskord_Front.Views"
    xmlns:models="using:Thiskord_Front.Models.Project"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="MessageViewTemplate" x:DataType="models:Message">
            <Grid Height="Auto" Margin="3">
                <StackPanel MinHeight="75" MaxWidth="650" Padding="5, 0, 5, 5" 
                            Background="{ThemeResource SystemColorHighlightColor}" 
                            CornerRadius="{StaticResource ControlCornerRadius}"
                            HorizontalAlignment="{x:Bind MsgAlignment}">
                    <StackPanel Orientation="Horizontal" Spacing="8" Padding="0, 10, 0, 4">
                        <TextBlock Text="{x:Bind MsgAuthor}" FontSize="14" FontWeight="SemiBold" Foreground="{ThemeResource SystemColorHighlightTextColor}"/>
                        <TextBlock Text="{x:Bind MsgDateTime}" FontSize="13" Opacity="0.75" Foreground="{ThemeResource SystemColorHighlightTextColor}"/>
                    </StackPanel>
                    <TextBlock Text="{x:Bind MsgText}" FontSize="20" Foreground="{ThemeResource SystemColorHighlightTextColor}" TextWrapping="Wrap" Padding="0, 0, 0, 0" RightTapped="Message_RightTapped">
                        <TextBlock.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Tag="edit" Text="Modifier" Icon="Edit" Click="MessageContextMenu_Click"/>
                                <MenuFlyoutItem Tag="delete" Text="Supprimer" Icon="Delete" Click="MessageContextMenu_Click"/>
                            </MenuFlyout>
                        </TextBlock.ContextFlyout>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView x:Name="InvertedListView"
              Grid.Row="0"
              ItemTemplate="{StaticResource MessageViewTemplate}">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView"
                                 VerticalAlignment="Bottom"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                 AutomationProperties.Name="multi-line TextBox"
                 TextWrapping="Wrap" 
                 IsSpellCheckEnabled="True"
                 PreviewKeyDown="TextBox_PreviewKeyDown"
                 SelectionHighlightColor="SteelBlue"
                 Margin="0,0,10,0"
                 x:Name="MessageInput"
                 />
            <Button Grid.Column="1"
                Content="Envoyer"
                VerticalAlignment="Bottom"
                Click="OnSendButton_Click" />
        </Grid>
    </Grid>
</Page>
